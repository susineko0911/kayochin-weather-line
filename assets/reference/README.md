# かよちん基準画像（非公開）

毎回の画像生成で同じ顔・髪型・髪色を保つため、基準画像を次の名前で置きます。

`kayochin_reference.png`

実際の画像ファイルは `.gitignore` の対象なので、公開GitHubリポジトリにはアップロードされません。ローカルで動くCodexの5:15自動処理から参照します。

おすすめの基準画像:

- 正面または斜め正面で、顔がはっきり見える
- 髪型・髪色が分かる
- 全身または膝上まで写っている
- 手や小物で顔が隠れていない
- 1人だけ写っている
